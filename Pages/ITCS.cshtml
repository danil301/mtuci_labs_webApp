@page
@model mtuci_labs.Pages.ITCSModel
@{
}

<h1>Методички для лабораторных работ по Введению в информационные технологии</h1>

<div class="container">
	@for (int i = 0; i < Model.Subjects.Count(); i++)
	{	
			@if(Model.Subjects[i].Name == "Введение в информационные технологии")
			{
				<div class="d-flex justify-content-between">
					<div>Предмет: @Model.Subjects[i].Name</div>
					<div>Курс: @Model.Subjects[i].Course</div>
					<a href="@Url.Page("Devops", "DownloadFile", new { fileName = Model.Files[i].FileName })">@Model.Files[i].FileName</a>
				</div>
			}		
	}
</div>